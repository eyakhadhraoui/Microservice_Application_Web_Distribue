spring.application.name=config-server
server.port=8888

# Config Server backend: "native" = fichiers locaux (pratique en dev).
spring.profiles.active=native
spring.cloud.config.server.native.searchLocations=classpath:/config-repo

