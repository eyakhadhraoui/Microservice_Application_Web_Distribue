# Nom du service dans le dashboard Eureka (http://localhost:8761)
spring.application.name=eureka-server
server.port=8761
# Le serveur Eureka ne s’enregistre pas comme client et ne récupère pas le registre
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
eureka.instance.hostname=localhost
