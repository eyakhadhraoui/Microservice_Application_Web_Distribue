package com.example.prescription_Service.config;

import org.springframework.context.annotation.Configuration;

/**
 * WebConfig — CORS géré entièrement par SecurityConfig.
 * Ne pas définir de bean corsConfigurer ici pour éviter les conflits.
 */
@Configuration
public class WebConfig {
    // Vide intentionnellement
}